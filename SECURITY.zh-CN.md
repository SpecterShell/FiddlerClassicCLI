# 安全策略

[English (en-US)](SECURITY.md) | **简体中文 (zh-CN)**

## 敏感抓包数据

Fiddler 会话可能包含凭据、Cookie、令牌、私密请求正文和个人信息。本项目原样保留调用方请求的数据，不会静默脱敏。请将 CLI 输出、MCP 对话记录、SAZ/FARX 文件和导出的正文文件作为敏感证据保护。

托管 MCP HTTP 默认禁用、仅绑定回环地址，身份验证模式为 `non-loopback`。它也可以绑定 IPv4 `0.0.0.0` 或最多 16 个选定的活动本地 IPv4 地址。启用远程访问前必须明确确认风险。远程模式使用明文 HTTP，任何监听到 Bearer 凭据的人都可以重复使用。请限制网络路径上的访问，并在不需要远程访问时优先使用回环地址。选定绑定保留具体地址，地址不可用时会报错，不会回退到更广的监听范围。

托管身份验证支持 `required`、`non-loopback`（新建或未设置配置的默认值）和 `none`。`required` 检查每个请求。`non-loopback` 先将 IPv4 映射地址转换为 IPv4，仅在实际套接字的远端和本地 IP 都是回环地址时跳过 Bearer 检查。即使请求来自本机，访问局域网地址仍须提供 Bearer 凭据。套接字地址未知时必须验证身份，`Host`、`Forwarded` 和 `X-Forwarded-For` 不能获得豁免。保存或启用 `none` 需要确认，因为所有可达客户端都将获得完整 MCP 权限，包括抓包流量和修改工具。本机进程（包括其他 Windows 账户下的进程）可以匿名访问回环 MCP，并通过默认 `non-loopback` 豁免获得相同的完整权限。命名管道仍仅允许当前 Windows 用户访问。通过回环连接的本地中继或反向代理会被视为回环对端。如需回环调用方或中继也验证身份，请选择 `required`。工具仍要求相应的确认参数，任何已连接客户端都可以提供这些参数。保存的凭据无法限制匿名访问。前台 `mcp http` 始终要求身份验证，并仅绑定回环地址。

修改托管服务的绑定、端口或身份验证前，必须先禁用服务。启动策略在守护进程启动和 Fiddler 加载扩展时生效。默认的 `last-state` 保留已保存的预期状态，`enabled` 和 `disabled` 则在启动时覆盖该状态。保存自动启用远程访问或 `none` 模式的策略需要明确确认。仅绑定回环地址时，保存或启用 `non-loopback` 无需访问风险确认。配置文件中已有的显式旧字段 `HttpRequireAuthentication` 在加载时将 `true` 转为 `required`，将 `false` 转为 `none`。

会话汇总包含主机名及活动计数，省略标头和正文。请将其视为敏感元数据。`doctor --output` 生成的诊断报告受白名单限制，仅包含数字版本、已知功能、监听状态和固定错误。报告不包含流量、凭据、客户端身份、局域网地址、本地路径或原始异常文本。公开分享前请检查报告内容。

## 本地信任边界

- Fiddler 桥接命名管道通过显式 ACL 限制为仅允许当前 Windows 用户访问。
- CLI 守护进程命名管道使用运行时提供的限制，仅允许当前用户访问。
- HTTP 配置和为兼容而保留的默认令牌仅允许当前 Windows 用户访问。命名客户端令牌只存储 SHA-256 哈希。
- 在 `required` 模式下，或请求不符合 `non-loopback` 豁免条件时，HTTP 请求必须携带 Bearer 凭据。`none` 模式对所有客户端跳过 Bearer 检查。前台和托管 HTTP 服务器在所有身份验证模式下都对包含 `Origin` 标头的请求返回 HTTP 403，即使凭据有效也会拒绝。为防止 DNS 重绑定，匿名请求的 Host 标头还必须与接收套接字的实际本地 IP 地址和端口一致，回环连接也可以使用 `localhost`。匿名请求不得使用自定义 DNS 名称。服务器不会启用 CORS，也不会配置 TLS、证书或 Windows 防火墙。
- `scripts/install.ps1` 默认写入 `%LOCALAPPDATA%\Programs\FiddlerClassicCLI`，更新当前用户的 `PATH`，将嵌入的桥接程序集部署到用户的 Fiddler Scripts 目录，并记录已安装主程序路径。`-NoPathUpdate` 和 `-SkipBridge` 分别跳过对应更改。
- Fiddler Classic 及其扩展以交互用户的权限运行。

引导脚本从选定的 GitHub 仓库下载 `fiddler-classic-cli.exe`，要求 GitHub 提供有效的文件 SHA-256 摘要。脚本在执行前验证下载内容，摘要缺失或不匹配时会停止。此检查依赖所选仓库和 GitHub 元数据的可信性。显式指定的本地 EXE 和目录作为可信输入处理。本地 ZIP 要求相邻的 `SHA256SUMS` 中有文件名完全一致且校验值匹配的条目。请仅运行可信来源的脚本和发布程序。更新会替换固定路径上的 CLI，并在未跳过桥接时部署桥接。安装不会启动 Fiddler、守护进程或 MCP 服务器，不会安装 Agent Skills，也不会更改证书信任和 HTTPS 解密设置。

以同一 Windows 用户身份运行的进程通常都可以访问该用户的文件和本地 IPC 端点。桥接无法通过沙箱隔离以该用户身份运行的恶意软件。

CLI 守护进程转发带版本的桥接协议，也可以运行托管 MCP HTTP 监听器。它只跟踪运行所需的连接元数据，不会保留请求 URL、标头、令牌、正文或抓包流量。守护进程不会启动 Fiddler，不会在启动时修改代理设置，也不会安装证书。

启用身份验证的 HTTP 监听器会在配置更改后重新加载默认及命名凭据。已撤销的凭据无法通过后续身份验证。取消授权会中止托管监听器中与该凭据关联的连接，但无法限制匿名访问、撤回已分派的操作或中止前台连接。匿名连接记录不包含令牌客户端身份。守护进程无响应时，管理命令不允许回退到离线操作。

MCP 客户端元数据（包括 `clientInfo` 和能力声明）是不可信的协议数据，不能授予权限或取代 Bearer 身份验证。现代 HTTP 请求要求协议版本和方法标头与正文一致，工具调用还要求工具名称标头一致。SDK 会在派发前拒绝不一致的值。

## 代理挂接与可达性

`capture start` 和 `capture stop` 将 Fiddler 挂接为运行它的 Windows 主机的系统代理或取消挂接。取消挂接后，运行中的 Fiddler 代理监听器仍可接收显式路由的流量。本机客户端可使用回环地址。远程客户端需要在 Fiddler 中配置远程访问，使其监听可达接口或所有接口，并确保路由和防火墙规则允许连接。IPv4 `0.0.0.0` 是监听所有接口的绑定地址，客户端应使用具体的回环地址或主机地址，并指定代理端口。

Fiddler 代理访问与 MCP HTTP 绑定及身份验证分别配置。抓包命令保留证书信任和 HTTPS 解密设置。CLI 不会添加网络路由或防火墙规则。停止系统代理抓包不会阻止显式使用 Fiddler 监听器的客户端。

## 流量修改

桥接会将 AutoResponder 匹配和操作字符串原样传递给 Fiddler。原生操作可以重定向、生成、延迟、丢弃或重置流量，也可以读取当前 Windows 用户有权访问的响应文件。启用规则前应检查 Agent 提议的操作和路径。

断点修改会在请求或响应继续处理前更改传输中的数据。托管触发器默认为一次性，由其暂停的流量会在有限保持时间后自动继续。桥接不对 Fiddler 中手动创建的暂停设置超时。移除触发器不会使已暂停的会话继续。

## 证书处理

本项目不会安装、生成或信任证书。HTTPS 解密需要在 Fiddler Classic 中显式配置。

## 破坏性操作

`app close` 和 `app restart` 需要确认，因为正常关闭 Fiddler 会停止当前捕获，并可能丢失未保存的会话。请先保存需要的证据。CLI 不会保存捕获记录、强制终止进程或处理原生对话框，只操作已验证且位于当前 Windows 会话、属于当前用户的进程。显式打开和重启使用 `-noattach`。重启不会恢复捕获记录或先前的捕获状态。关闭请求在超时或取消后仍可能完成，重试前请用 `app detect` 检查状态。

清空会话或规则、删除规则、替换 SAZ/FARX 文件或完整规则列表，以及中止暂停的会话都必须明确确认。托管 HTTP 还要求在启用远程访问或 `none` 模式、保存 `none` 模式、保存允许此类访问的启动设置、撤销凭据、断开连接，以及存在客户端连接时禁用服务之前确认。CLI 自动化必须按要求传递 `--yes`。MCP 调用方必须传递对应的确认参数。

撤销客户端凭据会中止与该凭据关联的连接。断开连接会中止选定的传输连接。已分派的操作可能在中止前就已完成。

## 报告安全问题

请勿在公开报告中包含抓包流量、令牌、SAZ 文件或其他秘密信息。请使用合成数据提供最小复现。
