# 安全策略

[English (en-US)](SECURITY.md) | **简体中文 (zh-CN)**

## 敏感抓包数据

Fiddler 会话可能包含凭据、Cookie、令牌、私密请求正文和个人信息。本项目会精确保留请求的数据，不会静默脱敏。请将 CLI 输出、MCP 对话记录、SAZ/FARX 文件和导出的正文文件视为敏感证据并妥善保护。

不要通过端口转发、反向代理、通配地址绑定或防火墙规则暴露 MCP HTTP 端点。唯一受支持的端点仅绑定回环地址，并要求 Bearer 令牌。

## 本地信任边界

- Fiddler 桥接命名管道通过显式 ACL 限制为当前 Windows 用户。
- CLI 守护进程命名管道使用运行时的仅限当前用户选项。
- HTTP 令牌及其配置目录仅允许当前 Windows 用户访问。
- HTTP 服务器绑定 `127.0.0.1`，且不启用 CORS。
- 桥接安装只写入当前用户的 Fiddler Scripts 目录。
- Fiddler Classic 及其扩展以交互用户的权限运行。

任何已以同一 Windows 用户身份运行的进程通常都可以访问该用户的文件和本地 IPC 端点。桥接不能隔离同一用户下的恶意软件。

CLI 守护进程只转发带版本的桥接协议，并支持本地状态和关闭控制。它不会启动 Fiddler、在启动时修改代理设置或安装证书。

## 流量修改

AutoResponder 匹配和操作字符串会原样传递给 Fiddler。原生操作可以重定向、生成、延迟、丢弃或重置流量，也可以读取当前 Windows 用户有权访问的响应文件。启用规则前应检查 Agent 提议的操作和路径。

断点修改会在请求或响应继续处理前改变传输中的数据。托管触发器默认为一次性，并在有限保持时间后自动继续。Fiddler 中手动创建的暂停没有桥接设置的超时。移除触发器不会继续已经暂停的会话。

## 证书处理

本项目不会安装、生成或信任证书。HTTPS 解密仍然是 Fiddler Classic 中需要显式启用的配置。

## 破坏性操作

清空会话或规则、删除规则、替换 SAZ/FARX 文件或完整规则列表，以及中止暂停的会话都必须明确确认。CLI 自动化需要在适用位置传递 `--yes`；MCP 调用方必须传递对应的确认参数。

## 报告安全问题

请勿在公开报告中包含已捕获流量、令牌、SAZ 文件或其他密钥。请使用合成数据提供最小复现。
